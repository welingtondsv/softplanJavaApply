import { NascimentoValidador } from './nascimento-validador';

describe('NascimentoValidador', () => {
  it('should create an instance', () => {
    expect(new NascimentoValidador()).toBeTruthy();
  });
});
